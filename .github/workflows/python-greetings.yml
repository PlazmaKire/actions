name: konveijers

on:
  workflow_dispatch:

jobs:
  install-pip-deps:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v3.5.2
      - uses:  actions/checkout@v3.5.2
        with:
          repository: 'mtararujs/python-greetings'
          path: 'app'
